

## Change Hamburger Button Backdrop to Purple

Update the mobile menu button in `src/components/Header.tsx` (line ~112) to use a purple-tinted backdrop instead of `bg-black/20`.

**Change:** Replace `bg-black/20` with `bg-primary/30` (or similar) so the pill uses the brand purple at partial opacity.

| File | Change |
|------|--------|
| `src/components/Header.tsx` | Line ~112: `bg-black/20` → `bg-primary/30` |

